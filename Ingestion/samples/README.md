# Samples

This folder keeps only the minimal examples still used by the repository.

## Files kept

- `weather_normalized_example.json`
- `air_quality_normalized_example.json`

Raw source snapshots were removed because they were no longer used by the active ingestion flow or validation scripts.
