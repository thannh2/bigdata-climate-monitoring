# Runtime dependencies for the current ingestion module.
# This set is intentionally minimal and matches the code currently used in Ingestion/.

# Kafka client
confluent-kafka==2.13.2

# HTTP and config
requests==2.32.5
PyYAML==6.0.3

# Data models
pydantic==1.10.22
