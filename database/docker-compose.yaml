mongodb:
    image: mongo:6.0
    container_name: mongodb
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    networks:
      - bigdata_net # Đảm bảo sử dụng chung network với Spark và Kafka

volumes:
  mongodb_data: